# Production environment variables
# These are baked into the JS bundle at build time

# ROM CDN URL - where SNES ROMs are hosted (Railway S3)
NEXT_PUBLIC_ROM_CDN_URL=https://cdn-hankshits.up.railway.app/roms
