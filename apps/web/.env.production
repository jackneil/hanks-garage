# Production environment variables
# These are baked into the JS bundle at build time

# ROM URL - proxied through Next.js API to avoid CORS issues
# The Railway CDN doesn't pass CORS headers, so we proxy instead
NEXT_PUBLIC_ROM_CDN_URL=/api/roms
